import React, { Fragment } from 'react';

const Search = () => {
    return (
        <Fragment>
            <form className="searchBox" on>

            </form>
        </Fragment>
    )
};

export default Search;
